<?xml version="1.0" encoding="utf-8"?>
<androidx.recyclerview.widget.RecyclerView
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        android:id="@+id/items"
        android:name="ru.mail.technotrack.ipfs.fragment.ItemsFragment"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginStart="@dimen/margin_standart"
        android:layout_marginEnd="@dimen/margin_standart"
        android:padding="@dimen/padding_standart_half"
        tools:context=".ui.fragment.DocumentsFragment"
        tools:listitem="@layout/fragment_document"/>